import React from 'react'

interface {{ pascalCase name }}Props {
  children?: React.ReactNode
}

function {{ pascalCase name }}({children}: {{ pascalCase name }}Props): JSX.Element {
  return (
    <div>
      <h1>{{ name }}</h1>
      {children}
    </div>
  )
}

export default {{ pascalCase name }}